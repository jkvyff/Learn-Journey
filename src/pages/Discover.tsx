import * as React from 'react'

interface Props { }

export const Discover: React.FC<Props> = () => {

    return (
        <div>
            <div>Discoveries to be made:</div>
        </div>
    );
}